import { styled } from 'styled-components';

export const MainSidebarStyled = styled('div')(() => ({
  position: 'relative',
}));

export const RotateSidebarSvg = styled('div')(() => ({
  position: 'absolute',
  top: 188,
  left: 27,
}));
